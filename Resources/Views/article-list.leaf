#extend("base")

#export("head") {
	<link rel="stylesheet" href="/styles/components/article.css">

	#if(isSearch || page > 1) {
		<meta name="robots" content="noindex, noarchive">
	}
}

#export("body") {
	#loop(posts, "post") {
		#embed("Partials/article")
	}
}

#export("after-content") {
	#if(showPagination) {
		#embed("Partials/page-navigation")
	}
}

#export("end-stylesheets") {
	#if(isSearch) {
		<link rel="stylesheet" href="/styles/components/search.css">
	}
}